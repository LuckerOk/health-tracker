// Core
import { FC } from 'react';

// Components
import { RegistrationComponent } from '../../bus/user';

export const Registration: FC = () => (
    <>
        <RegistrationComponent />
    </>
);
